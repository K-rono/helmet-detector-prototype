# Deployment-friendly requirements for Streamlit Cloud
# This version excludes OpenCV to avoid deployment issues

# Core dependencies
streamlit==1.37.1
Pillow==10.4.0
numpy

# ML dependencies - Using exact versions for model compatibility
tensorflow==2.19.0
keras==3.10.0
# ultralytics==8.0.196  # Excluded - causes OpenCV dependency issues in deployment
# Torch stack for RCNN (ensure you install matching CUDA builds if needed)
torch
torchvision==0.23.0

# PyTorch compatibility note:
# If using PyTorch 2.6+, the YOLO model loading has been patched for compatibility

# Note: opencv-python is excluded to avoid libGL.so.1 deployment issues
# The application works without OpenCV as it uses PIL for visualization
